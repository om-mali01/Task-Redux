CREATE TABLE tasks(
	id SERIAL PRIMARY KEY,
	title VARCHAR(20),
	description TEXT,
	status VARCHAR(20) DEFAULT 'pending',
	created_at TIMESTAMP DEFAULT NOW(),
	updated_at TIMESTAMP
);

CREATE TABLE users(
	id SERIAL PRIMARY KEY, 
	username VARCHAR(20),
	email VARCHAR(20),
	password VARCHAR(20)
);

CREATE TABLE task_assignments(
	id SERIAL PRIMARY KEY,
	task_id INTEGER REFERENCES tasks(id) ON DELETE CASCADE,
	user_id INTEGER REFERENCES users(id) ON DELETE CASCADE
);

INSERT INTO tasks (title, description, status, created_at, updated_at) 
VALUES 
    ('Buy Groceries', 'Purchase fruits and vegetables from the market', 'pending', NOW(), NOW()),
    ('Complete Project', 'Finalize the Redux project and deploy it', 'in-progress', NOW(), NOW()),
    ('Workout', 'Morning cardio and strength training session', 'completed', NOW(), NOW());